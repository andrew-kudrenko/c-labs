#include <stdio.h>

int main()
{
    int foo, bar;
    
    printf("The work has been done by Andrew Kudrenko, IT-202\n");
    
    printf("Enter two any integers\n"); 
    scanf("%d %d", &foo, &bar);
    printf("Sum of the numbers equals %d", foo + bar);
    
    return 0;
}
